# M5.6 正式执行记录

## 1. 记录目的

本文记录 M5.6 从离线准备、付费执行授权、正式运行到盲审材料发布的完整证据链。它不替代原始 journal、正式 Manifest 或盲审映射，也不在没有项目 owner 精确授权时触发真实 provider 调用。

## 2. 当前状态

- 阶段状态：已完成；正式 live、证据复核和盲审材料发布均通过退出门禁。
- 启动日期：2026-07-28。
- 完成日期：2026-07-28。
- 当前边界：M5.7 尚未启动；本文不包含条件级质量比较、确定性评分、人工评分或 H1-H5 结论。

## 3. 冻结身份

| 项目 | 冻结值 |
| --- | --- |
| Manifest | [`experiments/evidence/writer-v1/freeze-manifest.json`](../../experiments/evidence/writer-v1/freeze-manifest.json) |
| Manifest checksum | `211275d9312207fef02a8f15ee3f3a86bfe6f31c52337361b9f2666260fb7e1f` |
| Manifest 文件 SHA-256 | `d4d2d390467f47097db67540bcafaffc51c98152cd176b730855ebd8f1277ff1` |
| Source commit | `f0c75655bd3f8ccd1ce4e662e687fe0d50edc026` |
| Provider / model | Moonshot / `kimi-k2.6`，非思考模式，可变别名 |
| 计划规模 | 24 tasks × 2 conditions × 5 repetitions = 240 runs |
| 请求边界 | 预计 240 次请求，最多 480 次 attempt，单并发 |
| 预计费用 | `CNY 12875520 micros`（¥12.875520） |
| 本地硬上限 | `CNY 25751040 micros`（¥25.751040） |

## 4. 目录边界

以下目录均位于 E 盘。冻结源码、原始执行证据、公开盲审材料和私有条件映射相互分离：

| 用途 | 路径 |
| --- | --- |
| 冻结 detached worktree | `E:\\Agent-Factory-Formal-Source-20260728` |
| 正式原始证据 | `E:\\Agent-Factory-Formal-Evidence-20260728` |
| 公开盲审包 | `E:\\Agent-Factory-Formal-Review-20260728` |
| 私有条件映射 | `E:\\Agent-Factory-Formal-Mapping-20260728` |

原始模型输出、API key 和私有条件映射不得提交 Git。仓库只保存阶段报告、身份摘要、校验和和可复算路径说明。

## 5. 离线准备检查

| 检查 | 状态 | 证据 |
| --- | --- | --- |
| 当前 `main` 干净并与 `origin/main` 同步 | 通过 | 启动时 `HEAD` 为 `6dd25ee484c12589e784f37dc2b152488dbc65a7` |
| detached worktree 指向冻结 source commit | 通过 | `HEAD=f0c75655bd3f8ccd1ce4e662e687fe0d50edc026`，porcelain 为空 |
| CPython、OpenAI SDK 与 Manifest 环境一致 | 通过 | CPython `3.11.15`，OpenAI SDK `2.46.0` |
| Manifest 文件身份 | 通过 | 文件 SHA-256 为 `d4d2d390467f47097db67540bcafaffc51c98152cd176b730855ebd8f1277ff1` |
| Manifest `content-and-environment` 校验 | 通过 | verifier 返回 checksum `211275d9312207fef02a8f15ee3f3a86bfe6f31c52337361b9f2666260fb7e1f` |
| 正式 240-run preflight | 通过 | `tasks=24 repetitions=5 runs=120+120 requests=240/480 currency=CNY cost_micros=12875520/25751040` |
| 省略 `--allow-live` 的拒绝路径无持久化副作用 | 通过 | CLI exit code `2`，错误为 `live execution requires --allow-live`，证据根仍不存在 |

离线准备没有检查 API key 是否存在，没有创建 provider client，也没有访问 Moonshot。当前主环境已具备 Manifest 绑定的解释器和 SDK，本轮没有安装或更新 Python、依赖或 `uv` cache。`uv` 的默认 cache 探测仍指向 C 盘且不可用，因此正式执行不调用 `uv run`，而是直接复用已验证的 `E:\\Agent-Factory\\.venv\\Scripts\\python.exe`；正式证据只写入 E 盘外部目录。

离线拒绝命令使用了与未来 live 完全相同的 definition、plan、Manifest、experiment ID、货币和硬上限，只省略 `--allow-live`。因此下一次实际执行不得修改这些参数，只能在获得 owner 授权后加入该开关：

```powershell
& 'E:\Agent-Factory\.venv\Scripts\python.exe' -m experiments run-formal-live `
  --definition-root experiments\definitions\writer-v1 `
  --plan experiments\definitions\writer-v1\execution-plan.json `
  --manifest E:\Agent-Factory\experiments\evidence\writer-v1\freeze-manifest.json `
  --output-root E:\Agent-Factory-Formal-Evidence-20260728 `
  --allow-live `
  --confirm-experiment-id writer-validation-v1 `
  --confirm-currency CNY `
  --confirm-hard-cost-micros 25751040
```

## 6. Live 执行与盲审

### 6.1 Owner 付费执行授权

2026-07-28，项目 owner 在离线门禁全部通过后明确批准：

> 批准 M5.6 正式付费执行：接受 Manifest 211275d9312207fef02a8f15ee3f3a86bfe6f31c52337361b9f2666260fb7e1f、source commit f0c75655bd3f8ccd1ce4e662e687fe0d50edc026、Moonshot kimi-k2.6 非思考模式、完整 240-run、最多 480 次 attempt、单并发、预计费用 ¥12.875520 和本地硬上限 ¥25.751040；授权读取 MOONSHOT_API_KEY 并执行 run-formal-live --allow-live，若中断则使用同一证据目录恢复；本次不启动 M5.7。

该授权与第 3 节冻结身份、第 4 节目录边界和第 5 节精确命令一致，不授权部分执行或现场修改。

首次启动在访问 provider 前因进程和用户级环境均不存在 `MOONSHOT_API_KEY` 而以 exit code `3` 拒绝，正式证据根仍未创建，未产生请求或费用。项目 owner 随后单独批准从 `E:\\Agent-Factory-Pilot-Secrets\\moonshot-api-key.dpapi` 使用当前 Windows 用户 DPAPI 解密密钥；明文只允许注入 M5.6 执行子进程，执行后清除，不得输出或写入实验产物。

### 6.2 执行结果

正式进程使用一次连续 launcher session 完成，没有中断或恢复。PowerShell 在子进程退出后的 `finally` 中移除进程级 `MOONSHOT_API_KEY`，释放 DPAPI 解密缓冲区；密钥未写入仓库或实验产物。

| 项目 | 观测值 |
| --- | --- |
| 执行窗口 | `2026-07-28T13:16:28.698895Z` 至 `2026-07-28T14:13:21.047159Z` |
| Launcher | exit code `0` |
| Run 终态 | 240：`succeeded=232`，`invalid-response=8` |
| Provider attempts | 245 / 480，包含 5 次有限重试 |
| Attempt 状态 | `succeeded=232`，`invalid-response=8`，`provider-failed=5` |
| 错误码 | `MOONSHOT_NETWORK_ERROR=5`，`MOONSHOT_RESPONSE_INCOMPLETE=6`，`MOONSHOT_OUTPUT_NOT_JSON_OBJECT=1`，`MOONSHOT_OUTPUT_SCHEMA_INVALID=1` |
| Provider request ID | 240 个非空且互不重复；5 次 network error 未取得 ID，随后均按冻结策略重试 |
| Usage | 71,730 input + 66,820 output tokens；240 次取得 usage，5 次 network error 无 usage |
| 观测费用 | `CNY 2270385 micros`（¥2.270385） |
| 本地硬上限 | `CNY 25751040 micros`（¥25.751040），未触发 |
| 原始证据 | 973 个文件，46,763,544 bytes |
| Execution Manifest 文件 SHA-256 | `4375234eeeb2546c4d217fb92a5ac5cb041f31eb797878d97e6dbc04407abaa2` |

`ExperimentEvidenceLoader` 从固定 execution plan 重新加载并验证 240 个 request、245 组 started/completed attempt journal、240 个 terminal、来源 checksum、attempt 顺序、时间关系、usage 成对性和计划外文件拒绝，得到与 launcher 完全相同的聚合结果。上述 8 个无效响应和 5 次网络失败均保留，没有删除不利样本。

### 6.3 盲审材料

只有在完整 journal 通过 loader 后才调用 `build-blind-review`。公开包与私有映射使用两个不相同、互不嵌套的 E 盘根目录；相同命令第二次重放得到完全相同的 package 和 mapping checksum。

| 产物 | 结果 |
| --- | --- |
| 公开盲审包 | 240 items + 1 manifest，833,100 bytes |
| Package checksum | `ef5a31f9d0b5a81a169c22d3c68321fcc8723561f057e70f0a92a6880ce85fca` |
| Package Manifest 文件 SHA-256 | `50ff1e293175dcca0c32aef6803b535360db073663cc494d5dd958fa7093b856` |
| 私有映射 | 240 records，1 个文件，91,144 bytes |
| Mapping checksum | `64eeb21b7ab800458aea7b8db81baf405017c6a8926e116edff50131960c118b` |
| Mapping 文件 SHA-256 | `39880b5253d0fa4983a6f67d48d55cf9bd0d3cee292c34052906ca26a067bdf5` |
| 私有条件平衡 | `factory-agent=120`，`manual-agent=120` |

公开 `BlindReviewItem` 的类型契约不包含 condition、run ID、repetition、execution order、Prompt hash 或 AgentSpec；私有 mapping 保存从随机 review ID 恢复到来源 run 的完整链。人工评分尚未进行，也没有把私有映射放入公开目录。

## 7. 结论边界

M5.6 已完成正式原始证据和盲化评审材料的建立。当前只能断言 240-run 按冻结协议执行、失败证据被保留、费用受控且盲审来源可恢复；不能从总体成功率或个别输出推断工厂条件优于或劣于手写条件。H1-H5 的评分、统计检验和结论属于尚未启动的 M5.7。
